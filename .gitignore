# Ignora tutti i file
*

# Non ignorare le directory (altrimenti Git non entra nelle cartelle)
!*/

# Non ignorare i file che hanno un'estensione (es. foo.txt, script.py)
!*.*

# (Opzionale) Non ignorare dotfiles importanti come .gitignore stesso
!.* 
